<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/white">

    <!-- Ảnh địa điểm -->
    <ImageView
        android:id="@+id/imagePlace"
        android:layout_width="match_parent"
        android:layout_height="280dp"
        android:scaleType="centerCrop"
        android:src="@drawable/langbac" />

    <!-- Nội dung chi tiết -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Tiêu đề địa điểm -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/textTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Lăng Chủ Tịch Hồ Chí Minh"
            android:textStyle="bold"
            android:textSize="22sp"
            android:textColor="@color/black"
            android:fontFamily="@font/lobster"
            android:layout_marginBottom="8dp" />

        <!-- Địa điểm với icon -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="16dp"
            android:gravity="center_vertical">

            <ImageView
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:src="@drawable/ic_location"
                app:tint="#666666" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/textLocation"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:text="Ba Đình, Hà Nội"
                android:textSize="14sp"
                android:textColor="#666666" />
        </LinearLayout>

        <!-- Mô tả chi tiết -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/textDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Lăng Chủ tịch Hồ Chí Minh là công trình trang nghiêm, linh thiêng, nơi yên nghỉ của Chủ tịch Hồ Chí Minh - vị lãnh tụ kính yêu của dân tộc Việt Nam. Đây là điểm đến không thể bỏ qua khi du lịch Hà Nội."
            android:textSize="15sp"
            android:lineSpacingExtra="3dp"
            android:textColor="@color/black"
            android:layout_marginBottom="24dp" />

        <!-- Spacer để đẩy button xuống dưới -->
        <View
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- Buttons Container -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:weightSum="2">

            <!-- Nút Add to Favorite -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnAddToFavorite"
                android:layout_width="0dp"
                android:layout_height="50dp"
                android:layout_weight="1"
                android:layout_marginEnd="8dp"
                android:text="🤍"
                android:textAllCaps="false"
                android:textColor="@color/white"
                android:textSize="20sp"
                android:textStyle="bold"
                app:backgroundTint="#E91E63"
                app:cornerRadius="25dp" />

            <!-- Nút Directions -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnDirections"
                android:layout_width="0dp"
                android:layout_height="50dp"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:text="🧭 Directions"
                android:textAllCaps="false"
                android:textColor="@color/white"
                android:textSize="14sp"
                android:textStyle="bold"
                app:backgroundTint="#00BCD4"
                app:cornerRadius="25dp" />

        </LinearLayout>

    </LinearLayout>

    <!-- Bottom Navigation -->
    <include layout="@layout/layout_bottom_menu" />

</LinearLayout>